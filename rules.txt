# ennicen-guardian synced cosmetic rules — one CSS selector per line.
# Appended by the userscript's "Zap an element…" picker (each zap is a commit here).
# Read by every device (unauthenticated, via raw.github); only devices with a
# GitHub token can append. Each selector is hidden with `display: none`.
# `# ...` is an inline comment and is stripped before use.
#
# A line of the form `keep: <selector>` is the opposite: it tells the picker to
# stop offering that block (the "✓ keep" pill). It hides nothing.
#
# A line of the form `boring: <regex>` hides any headline card whose text matches
# the JS regex (e.g. `boring: Prince Harry`). Use `\b` for word boundaries.
keep: section#news
keep: main#maincontent
section#world-cup-2026
section#what-to-play
section#fashion-\&-beauty
section#newsletters
section#secure-messaging
boring: \bDuchess\b
boring: \bDuke\b
boring: \bElon\b
boring: \bJoe Rogan\b
boring: \bPrince Harry\b
boring: \bPrince William\b
boring: \bRoyal\b
boring: \bTwitter\b
boring: \bfootball\b
boring: \bfootballer\b
boring: \bWorld Cup\b
div[data-link-name='more-on-this-story']
keep: div#container-news
section#more-sport
div#expander-5480186\ form
div#discussion-overlay
keep: div#article
gu-island[name='MostViewedRightWithAd']
